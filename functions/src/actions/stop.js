/**
 * Handle Alexa Amazon.StopIntent
 * Assistant doesn't touch this event
 * stop and exist are synonymous intents
 *
 * @type {{handler: handler}}
 */
module.exports = Object.assign({}, require('./exit'));
